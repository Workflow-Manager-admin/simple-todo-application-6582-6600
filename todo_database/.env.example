# Configuration for todo_database backend

# Path to SQLite DB file (will be created if doesn't exist)
TODO_DB_PATH=todos.db

# The port the backend service will run on (default: 4000 if unset)
PORT=4000
